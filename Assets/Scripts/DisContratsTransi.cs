using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DisContratsTransi : MonoBehaviour
{
    public GameObject congratsTransiP;

    public void DisableCongratsTransi()
    {
        congratsTransiP.SetActive(false);
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
